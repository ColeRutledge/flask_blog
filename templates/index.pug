extends base.pug
block content
    //- h1 Hello World! I have been seen #{count} times.
    h1 Hello, #{user.username}!
    each post in posts
        div: p #{post.author.username} says: #[strong #{post.body}]
