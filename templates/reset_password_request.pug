extends base.pug
block content
    h1 Reset Password
    form(action='' method='post')
        =form.hidden_tag()
        p #{form.email.label} #[br] #{form.email(size=64)} #[br]
        for error in form.email.errors
            span(style='color: red;') #{error}
        p #{form.submit}
