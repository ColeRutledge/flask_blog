table.table.table-hover
    tr(valign='top')
        td(width='70px')
            a(href=url_for('user', username=post.author.username)): img(src=post.author.avatar(70))
        td
            #[a(href=url_for('user', username=post.author.username)) #{post.author.username}]
            | #{_('said')} #{moment(post.timestamp).fromNow()}: #[br] #{_(post.body)}
