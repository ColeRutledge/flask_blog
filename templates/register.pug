extends base.pug
{% import 'bootstrap/wtf.html' as wtf %}
block app_content
    h1 Register
    .row
        .col-md-4
            {{ wtf.quick_form(form) }}

//- BEFORE USING Flask-Bootstrap

//- form(action='' method='post')
//-     =form.hidden_tag()

//-     p #{form.username.label} #[br] #{form.username(size=32)}
//-     each error in form.username.errors
//-         span(style='color: red;') [#{error}]

//-     p #{form.email.label} #[br] #{form.email(size=32)}
//-     each error in form.email.errors
//-         span(style='color: red;') [#{error}]

//-     p #{form.password.label} #[br] #{form.password(size=32)}
//-     each error in form.password.errors
//-         span(style='color: red;') [#{error}]

//-     p #{form.password2.label} #[br] #{form.password2(size=32)}
//-     each error in form.password2.errors
//-         span(style='color: red;') [#{error}]

//-     p #{form.submit()}
